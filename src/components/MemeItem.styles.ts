import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    paddingHorizontal: 16,
    paddingBottom: 12,
    alignItems: 'center'
  },
  text: {
    paddingTop: 8,
    fontSize: 20
  }
})
