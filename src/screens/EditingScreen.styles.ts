import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  inputsContainer: {
    marginTop: 12,
    paddingHorizontal: 8
  },
  inputWrapper: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 8
  },
  text: {
    fontSize: 18,
    marginRight: 12
  },
  input: {
    fontSize: 18,
    flex: 1
  }
})
